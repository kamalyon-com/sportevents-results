# RaceResult API key — required for the live results widget
# Get this from your RaceResult software: Basic Settings → Access Rights / API Keys
# This key is embedded in the compiled JS bundle, so use a read-only results key only.
REACT_APP_RR_API_KEY=your_api_key_here
